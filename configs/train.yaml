defaults:
  - _self_
  - datamodule: road_data
  - model: mini_unet
  - logger: mlflow

trainer:
  accelerator: auto
  devices: 1
  max_epochs: 1
  log_every_n_steps: 5
  fast_dev_run: false

plots:
  out_dir: ./plots

production:
  export_onnx: true
